[pytest]
markers =
    unit: Fast isolated tests with all external calls mocked. No I/O.
    integration: Tests using fixture files and real logic. No live external services.
    smoke: End-to-end tests that call live APIs. Not run in CI.
testpaths = tests
